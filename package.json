{
  "name": "mt-app",
  "version": "1.0.0",
  "description": "vue-koa-eleui",
  "author": "poland5",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server --exec babel-node",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js --exec babel-node",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "babel-preset-es2015": "^6.24.1",
    "cross-env": "^5.2.0",
    "element-ui": "^2.4.11",
    "koa": "^2.6.2",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "node-sass": "^4.13.1",
    "nodemon": "^1.18.9",
    "sass-loader": "^7.2.0"
  }
}
